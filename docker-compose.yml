version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: popli-postgres
    environment:
      POSTGRES_USER: popli_user
      POSTGRES_PASSWORD: popli_password
      POSTGRES_DB: popli_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: popli-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
