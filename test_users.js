const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const users = await prisma.user.findMany({ select: { id: true, username: true } }); console.log(JSON.stringify(users, null, 2)); prisma.$disconnect(); } run();
